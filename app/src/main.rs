use lambda_runtime::{
    //service_fn, 
    //LambdaEvent, 
    Error};
//use serde::{Deserialize, Serialize};

#[tokio::main]
async fn main() -> Result<(), Error>{
    println!("Hello, world!!");
    Ok(())
}
